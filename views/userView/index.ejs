<section class="control-group">
  <%if (list.count > 0){%>
  <table>
    <thead>
    <tr>
      <th>
        Prénom
      </th>
      <th>
        Nom
      </th>
      <th>
        Pseudo
      </th>
      <th>
        Login
      </th>
      <th></th>
    </tr>
    </thead>
    <tbody>


    <% _.each(list, function(user) {%>

    <tr>

      <td><%=user.firstname%></td>
      <td><%=user.lastname%></td>
      <td><%=user.displayName%></td>
      <td><%=user.login%></td>
      <td>
        <a href="http://localhost:1337/user/detail/<%=+user.id%>" class="glyphicon glyphicon-eye-open">

        </a>
      </td>
    </tr>

    <% }) %>

    </tbody>
  </table>

  <% }else{ %>

  <article>
    <p>Aucun utilisateur</p>
    <a href="/user/create" class="glyphicon glyphicon-plus"></a>
  </article>
   <%}%>

</section>
